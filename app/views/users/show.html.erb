
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= "ブロック、トーク" %>
    <%= form_for @room do |f| %>
  		<div><%= f.hidden_field :friend_id%></div>
  		<%= f.submit "トーク", class: "btn btn-primary" %>
		<% end %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>


